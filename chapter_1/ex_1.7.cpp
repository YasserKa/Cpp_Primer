#include <iostream>

int main()
{

    /**  /* */std::cout << "The sum of " << v1;
              << " and " << v2;
              << " is " << v1 + v2 << std::endl;
        It's illegal since the input operator (<<) doesn't have an ostream as a left operand.
        This can be solved by either removing the ";"s or adding ostreams at the start of each line
    */
    return 0;
}
