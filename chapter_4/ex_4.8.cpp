/**
 * The left operand is evaluted first in case of AND and OR where it's the right operand
 * in case of equality.
 * If the first operand was true the right operand is evaluated in case of AND where it's 
 * ignored in case of OR.
 * If the first operand wa false the right operand is evaluated in case of OR where it's
 * ignored in case of AND.
 * Both operators are going to evaluated in case of equality.
 */

