/**
 * (a) do {
 *     int v1, v2;
 *     cout << "Please enter two numbers to sum:" ;
 *     if (cin >> v1 >> v2)
 *         cout << "Sum is: " << v1 + v2 << endl;
 * 		} while (cin);
 * 	sums 2 integers
 *  (b) do {
 *        // ...
 *        } while (int ival = get_response());
 *        get ival until it's 0
 *  (c) int val;
 *  do {
 *         ival = get_response();
 *     } while (ival);
 *     get ival until it's 0
 */

