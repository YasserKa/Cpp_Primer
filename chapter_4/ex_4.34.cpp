/**
 * (a) if (fval) fval becomes bool
 * (b) dval = fval + ival;  ival becomes float and the result will become double
 * (c) dval + ival * cval; cval becomes integer then the results becomes double
 */

