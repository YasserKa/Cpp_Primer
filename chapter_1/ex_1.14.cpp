/**
 * In a while loop you get only a boolean expression to check on
 * while a for loop got an init statement, bool expression and a post expression
 *
 * Both types can be used but it's more favorable to use the one that makes the
 * code more readable
 */
