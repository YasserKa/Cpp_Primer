/**
 * string s = "word";
 * string pl = s + s[s.size() - 1] == 's' ? "" : "s" ;
 * Arithmetic (+) operator is going to be evaluated first and there would be an error
 * when comparing a string with a character
 */

