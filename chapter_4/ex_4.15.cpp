/**
 * double dval; int ival; int *pi;
 * dval = ival = pi = 0;
 * It's illegal because pi is a pointer and it can't be used to assign an int or double
 * It can be fixed by assigning pi seperatly from the others
 * dval = ival = 0;
 * pi = 0;
 */

